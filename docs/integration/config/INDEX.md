# Module Integration Config

Le module `integration/config` gère le chargement et la résolution de `integration.yaml`.

## Fichiers

```{toctree}
:maxdepth: 1

loader
```

## Contribution

- Toute nouvelle section YAML doit être ajoutée dans les dataclasses de `loader.py`.
- Maintenir la compatibilité des valeurs par défaut.
